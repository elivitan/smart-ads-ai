
// app/routes/app.api.subscription.js // ══════════════════════════════════════════════ // Handles plan selection — validates and saves to DB. // // SECURITY NOTE: In production, plan changes should go through // Shopify Billing API (AppSubscriptionCreate mutation) so Shopify // handles payment. This endpoint should only be called AFTER // Shopify confirms the subscription. // // TODO: Replace direct plan update with Shopify Billing flow: //   1. Client calls this API with desired plan //   2. Server creates AppSubscriptionCreate mutation //   3. Shopify redirects user to payment confirmation //   4. On confirmation webhook, update DB via updatePlan() // ══════════════════════════════════════════════  import { updatePlan, getSubscriptionInfo } from "../license.server.js";  const VALID_PLANS = ["free", "starter", "pro", "premium"];  export const action = async ({ request }) => {   const { session } = await authenticate.admin(request);   const shop = session.shop;    try {     const body = await request.json();     const { plan } = body;      if (!plan || !VALID_PLANS.includes(plan)) {       return Response.json(         { success: false, error: `Invalid plan. Must be one of: ${VALID_PLANS.join(", ")}` },         { status: 400 }       );     }      // TODO: For paid plans, validate through Shopify Billing API first     // For now, directly update (development mode only)     if (plan !== "free") {       console.warn(`[SmartAds] WARNING: Plan "${plan}" set for ${shop} without Shopify Billing verification. Add billing flow before production!`);     }      await updatePlan(shop, plan, {       trial: plan !== "free",       trialDays: 7,     });      const info = await getSubscriptionInfo(shop);     return Response.json({ success: true, subscription: info });   } catch (err) {     console.error("[SmartAds] Subscription error:", err);     return Response.json({ success: false, error: err.message }, { status: 500 });   } };  // GET — return current subscription info export const loader = async ({ request }) => {
  const { authenticate } = await import("../shopify.server");   const { session } = await authenticate.admin(request);   const shop = session.shop;    try {     const info = await getSubscriptionInfo(shop);     return Response.json({ success: true, subscription: info });   } catch (err) {     return Response.json({ success: false, error: err.message }, { status: 500 });   } };
