const fs=require('fs');let c=fs.readFileSync('app/routes/app._index.jsx','utf-8');c=c.replace('isPaidServer ? planFromCookie : ((() => { try { return sessionStorage.getItem(\sai_plan\) || null; } catch { return null; } })())','serverSubscription?.plan && serverSubscription.plan !== \ree\ ? serverSubscription.plan : null)');fs.writeFileSync('app/routes/app._index.jsx',c);console.log(c.includes('planFromCookie')?'STILL THERE':'Fixed!')
